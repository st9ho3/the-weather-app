import React, { Children } from 'react'

const CurrentWeatherMid = ({children}) => {
  return (
    <div className='currentWeatherMid'>
      {children}
    </div>
  )
}

export default CurrentWeatherMid
